[project]
name = "missingfcup"
version = "0.1.0"
description = "Interactive missing-data visualizations"
authors = [{name="Matias"}]
requires-python = ">=3.10"

dependencies = [
    "pandas>=2.0",
    "plotly>=5.20"
]
